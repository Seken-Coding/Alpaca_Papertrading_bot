# ═══════════════════════════════════════════════════════════════════════════════
#                        ALPACA PAPER TRADING BOT
#                         Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
#
#  Copy this file to .env and fill in your credentials:
#    cp .env.example .env
#
#  NEVER commit your .env file — it contains secrets.
#


# ─────────────────────────────────────────────────────────────────────────────
#  1. MULTI-ACCOUNT API KEYS  (multi_main.py)
# ─────────────────────────────────────────────────────────────────────────────
#  Each account maps to an entry in config/accounts.yaml.
#  Uncomment and fill in one pair per paper-trading account.
#
#  Account mapping:
#    ACCT1  ->  momentum_aggressive   (intraday scanner)
#    ACCT2  ->  cest_conservative     (CEST daily strategy)
#    ACCT3  ->  cest_aggressive       (CEST daily + pyramiding)

ACCT1_API_KEY=
ACCT1_SECRET_KEY=

ACCT2_API_KEY=
ACCT2_SECRET_KEY=

ACCT3_API_KEY=
ACCT3_SECRET_KEY=


# ─────────────────────────────────────────────────────────────────────────────
#  2. SINGLE-ACCOUNT API KEYS  (main.py / cest_main.py standalone)
# ─────────────────────────────────────────────────────────────────────────────
#  Only needed if running main.py or cest_main.py directly (not multi_main.py).
#  You can leave these blank when using multi-account mode.

#ALPACA_API_KEY=
#ALPACA_SECRET_KEY=
ALPACA_PAPER=true


# ─────────────────────────────────────────────────────────────────────────────
#  3. INTRADAY BOT  (main.py)
# ─────────────────────────────────────────────────────────────────────────────

# -- Automation --
# true = place orders automatically
AUTO_EXECUTE=false
# minutes between scans while market is open
SCAN_INTERVAL_MIN=5
# max bracket orders per scan cycle
MAX_ORDERS_PER_SCAN=5

# -- Position sizing --
# max equity % in a single position (8%)
MAX_POSITION_PCT=0.08
# equity % risked per trade (2%)
RISK_PER_TRADE_PCT=0.02
# stop-loss = N x ATR below entry
ATR_STOP_MULTIPLIER=1.8
# minimum reward:risk ratio
MIN_RISK_REWARD=1.2

# -- Portfolio limits --
# max concurrent open positions
MAX_POSITIONS=20
# min $ buying power to allow new orders
MIN_BUYING_POWER=500.0

# -- Circuit breakers --
# halt if down 4% from session start
MAX_DAILY_LOSS_PCT=0.04
# halt if down 12% from session peak
MAX_DRAWDOWN_PCT=0.12
# signal quality filter (0-100)
MIN_SCORE_THRESHOLD=45.0

# -- Position monitor --
# auto-manage open positions every 60s
POSITION_MONITOR=true
# trailing stop % below high-water mark
TRAILING_STOP_PCT=1.0
# force-close after N calendar days (0=off)
MAX_HOLD_DAYS=5

# -- Market regime filter --
# block BUY when SPY < 200-SMA
REGIME_FILTER=false

# -- Scan window (US/Eastern) --
# skip first 30 min (chaotic open)
SCAN_START_ET=10:00
# skip last 30 min (rushed close)
SCAN_END_ET=15:30

# -- Stock universe --
# "static" = 50-symbol S&P sample, "dynamic" = discover via Alpaca API
UNIVERSE=static
# cache dynamic list for 24h (seconds)
UNIVERSE_CACHE_TTL=86400


# ─────────────────────────────────────────────────────────────────────────────
#  4. CEST BOT  (cest_main.py)
# ─────────────────────────────────────────────────────────────────────────────

#BROKER=alpaca                      # "alpaca" or "ib"
#DAILY_RUN_TIME=15:55               # daily cycle time (ET), 5 min before close


# ─────────────────────────────────────────────────────────────────────────────
#  5. LOGGING
# ─────────────────────────────────────────────────────────────────────────────
#  On a VPS, set an absolute path so logs land in a fixed location.
#  Leave blank to use the default relative "logs/" directory.

#LOG_DIR=/opt/trading-bot/logs
